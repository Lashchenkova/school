<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8" />
    <title>School</title>
    {% stylesheets 'bundles/app/css/school.css' filter='cssrewrite' %}
        <link rel="stylesheet" href="{{ asset_url }}" />
    {% endstylesheets %}
</head>
<body>
<header>
    <div class="head">
        <div class="login">
            <form action="{{ path('login') }}" method="post">
                <input type="text" id="username" name="_username" value="{{ last_username }}" placeholder="Email" required/>
                <input type="password" id="password" name="_password" placeholder="Password" required/>
                <button type="submit" class="btn btn-primary btn-block btn-large">LOGIN</button><br>
                <input type="checkbox" id="remember_me" name="_remember_me" />
                <label for="remember_me">Remember me</label>
                <a id="forget_password" href="">Forgot your password?</a>

                <span>use the following data to login as<br>
                <b>admin: </b>admin@mail.com<br>
            <b>teacher: </b>teacher@mail.com<br>
            <b>student: </b>student@mail.com<br>
            password: <b>12345</b></span>
            </form>
        </div>
    </div>
</header>
<div class="sliders">

    {% if error %}
        <div class="alert-warning">{{ error.messageKey|replace({"Invalid credentials." : "Invalid email or password"}) }}</div>
    {% endif %}

    <div class="quote">
        <h1>Education</h1>
        <h2>is the most powerful weapon which you can use to change the world.</h2>
        <h3>Nelson Mandela</h3>
    </div>
</div>
</body>

